
# Kurs Tracker - Extreme Edition

Интерактивный трекер прогресса с элементами геймификации.

## Новые функции
1. **Гоночная трасса (Race Track)**: Визуализация прогресса в виде гонки. Каждая пройденная тема двигает ваш болид `🏎️` ближе к финишу.
2. **Админ-панель**:
   - Переключается кнопкой в шапке.
   - Позволяет добавлять новых студентов.
   - Позволяет удалять студентов из списка.
3. **Сохранение данных**: Теперь все изменения сохраняются в `localStorage` вашего браузера.

## Технические детали
- Анимация болидов реализована через `transition: left` с расчетом `(count / 11) * 100%`.
- Управление состоянием (CRUD) вынесено в `App.tsx`.
